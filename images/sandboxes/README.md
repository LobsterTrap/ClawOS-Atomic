# Sandbox Images

Curated OpenShell sandbox image definitions live here. MVP 1 should prefer
trusted Project Hummingbird images directly when they satisfy a profile, and
add thin ClawOS-derived images only for integration glue.
