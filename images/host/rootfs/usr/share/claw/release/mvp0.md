# ClawOS Atomic MVP 0

This image is a developer preview scaffold. It installs the early ClawOS host
integration placeholders and reserves service names, users, state directories,
and recovery entry points for later MVP implementation.

The placeholder services are intentionally not enabled by default.
